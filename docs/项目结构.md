e:\my-web-project\
├── app/                        # 应用代码（多应用模式）
│   ├── api/                    # ★ API 应用（写接口的地方）
│   │   ├── controller/v1/      #   控制器（薄层：接参数→调服务→返回）
│   │   │   ├── AuthController.php
│   │   │   ├── UserController.php
│   │   │   └── admin/          #   管理员控制器（需 admin 角色）
│   │   ├── middleware/         #   中间件（JwtAuth/Cors/Throttle/Permission）
│   │   └── route/app.php       #   ★ API 路由（/api/v1/* 定义在这里）
│   ├── common/                 # 共享代码（api 和 index 都能用）
│   │   ├── lib/ApiResponse.php #   统一响应体
│   │   ├── exception/          #   业务异常类
│   │   ├── service/            #   ★ 业务逻辑层（相当于你的 m 目录）
│   │   └── validate/           #   参数验证器
│   ├── BaseController.php      # 控制器基类
│   └── ExceptionHandle.php     # 全局异常处理
├── config/                     # ★ 配置文件（框架约定，按文件名区分）
├── db/                         # ★ 数据访问层（相当于数据库实例封装）
├── route/app.php               # 根路由（非 API 请求的 SPA 回退）
├── frontend/                   # ★ 前端源码（Vue3，构建后到 public/app/）
├── public/                     # ★ Web 根目录（PHP 入口 + 构建产物）
├── docker/                     # 容器配置
├── docs/                       # 文档（需求、许可）
├── runtime/                    # 运行时缓存/日志（自动生成，勿手动改）
├── vendor/                     # Composer 第三方依赖（自动生成）
└── extend/                     # PSR-0 第三方类库（目前空，框架预留）
